{
  "policyVersion": "privacy-guard-policy/v1",
  "pathRules": [],
  "contentRules": [
    {
      "id": "windows-user-profile-path",
      "reason": "Tracked content must not contain committed Windows user-profile paths.",
      "regex": "C:\\\\Users\\\\[A-Za-z0-9_][A-Za-z0-9._-]*"
    },
    {
      "id": "windows-user-profile-path-forward-slash",
      "reason": "Tracked content must not contain committed Windows user-profile paths written with forward slashes.",
      "regex": "C:/Users/[A-Za-z0-9_][A-Za-z0-9._-]*"
    },
    {
      "id": "unix-macos-user-profile-path",
      "reason": "Tracked content must not contain committed macOS user-profile paths.",
      "regex": "/Users/[A-Za-z0-9_][A-Za-z0-9._-]*"
    },
    {
      "id": "unix-home-path",
      "reason": "Tracked content must not contain committed Unix home-directory paths.",
      "regex": "/home/[A-Za-z0-9_][A-Za-z0-9._-]*"
    }
  ],
  "contentAllowRules": [
    {
      "id": "public-kademlia-paper-url",
      "reason": "Allow the public Stanford Kademlia paper URL; it is a legitimate reference, not a personal home-directory leak.",
      "regex": "https://www\\.scs\\.stanford\\.edu/~dm/home/papers/kpos\\.pdf"
    },
    {
      "id": "public-kademlia-paper-url-policy-entry",
      "reason": "Allow the policy entry that documents the public Stanford Kademlia paper URL exception.",
      "regex": "schemas/privacy-guard/policy\\.v1\\.json:.*public Stanford Kademlia paper URL|schemas/privacy-guard/policy\\.v1\\.json:.*kpos"
    }
  ]
}
